###########################################################
# Compose / PostgreSQL only.
# The API reads apps/api/.env; the dashboards read their own .env.
###########################################################
DB_NAME=saas_store
DB_USERNAME=postgres
DB_PASSWORD=postgres
POSTGRES_HOST_PORT=5432
